<?xml version="1.0" encoding="utf-8"?>
<!--
    M3-flavored settings screen: surface-colored top app bar (no hard
    primary block) over a sand background, with preferences grouped
    into cards by PreferenceCategory.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface"
    android:orientation="vertical">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorSurface"
        android:elevation="0dp"
        android:theme="@style/ThemeOverlay.PocketHLE.AppBar"
        app:popupTheme="@style/ThemeOverlay.PocketHLE.AppBar" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="?attr/colorSurfaceVariant" />

    <FrameLayout
        android:id="@+id/preferences_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="?attr/colorSurface" />
</LinearLayout>
